<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>To Do List</title>
  </head>
  <body>
    <h1>Item Index Page</h1>
    <nav>
      <div><a href="/todo/new">Create a New Item</a></div>
      <div><a href="/">Go back to Main</a></div>
    </nav>
    <ul>
      <% for(let i = 0; i < tasks.length; i++){ %>
      <li>
        <a href="/todo/<%=tasks[i]._id%>">
          <%=tasks[i].name; %> at <%=tasks[i].time; %>.</a
        >
        <form action="/todo/<%=tasks[i]._id%>?_method=DELETE" method="POST">
          <input type="submit" value="DELETE" />
        </form>
        <a href="/todo/<%=tasks[i]._id%>/edit">Edit Task</a>
      </li>
      <% } %>
    </ul>
  </body>
</html>
